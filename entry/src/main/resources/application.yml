server:
  port: 8084
spring:
  application:
      name: entry-service
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8082/eureka # Eureka Server注册服务的地址

